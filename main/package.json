{
  "name": "main",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc && concurrently \"tsc -w\" \"wait-on dist/index.js && electron .\"",
    "build": "tsc",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^28.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  }
}
